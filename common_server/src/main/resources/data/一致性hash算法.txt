为什么要使一致性Hash算法：
答：1.均衡分布式（（hash）key%集群节点数）存在致命缺点：假设搞促销，临时添加一个节点，比如说之前的节点数是3，200%3为2，数据存在2号节点上，现在添加一台，200%4为0，0号节点就找不到之前2号的数据啦，
为此就要重新查询数据库，就活存在3/4（1-4/（3*4））的比列数据缓存不命中，瞬时失去缓存的分压，数据库不堪重压，奔溃，造成缓存雪崩（整个缓存系统垮掉）。
    2.j均衡分布式二：一致性hash算法

