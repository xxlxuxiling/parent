spring:
  cloud:
    config:
      #开启配置服务发现
      enabled: true
      discovery:
        #配置服务实例名称
        service-id: configServer
      #配置文件所在分支
      label: master
      profile: dev
      #仓库名称
      name: application